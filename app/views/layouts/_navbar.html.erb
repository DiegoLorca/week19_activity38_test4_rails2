<nav class="navbar navbar-expand-lg navbar-dark bg-dark">
  <a class="navbar-brand" href="<%= root_path %>">Bucket List del 18º</a>
  <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
    <span class="navbar-toggler-icon"></span>
  </button>

  <div class="collapse navbar-collapse" id="navbarSupportedContent">
    <ul class="navbar-nav mr-auto">
      <% if user_signed_in? %>
        <li class="nav-item">
          <span class="nav-link disabled">
            Completadas: <%= current_user.events.where(completed: true).count %> / <%= Task.count %>
          </span>
        </li>
      <% end %>
    </ul>
    <ul class="nav navbar-nav navbar-right">
        <li class="dropdown">
          <a href="#" id="navbarDropdown" class="nav-link dropdown-toggle" data-toggle="dropdown" role="button" aria-haspopup=“true” aria-expanded="false">
            <% if user_signed_in? %>
              <%= current_user.name %>
            <% else %>
              Ingreso
            <% end %>
             <span class="caret"></span></a>
          <ul class="dropdown-menu" role="menu">
            <% if user_signed_in? %>
              <li><a href="<%= destroy_user_session_path %>" data-method="DELETE">Salir</a></li>
            <% else %>
              <li><a href="<%= new_user_session_path %>">Ingreso</a></li>
              <li><a href="<%= new_user_registration_path %>">Registro</a></li>
            <% end %>
          </ul>
        </li>
      </ul>
  </div>
</nav>
